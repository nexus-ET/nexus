import DailyTriviaCard from '../nexus-intel/DailyTriviaCard';

/** Dashboard-facing wrapper for the daily trivia widget. */
const NexusIntelTriviaWidget: React.FC = () => {
  return <DailyTriviaCard />;
};

export default NexusIntelTriviaWidget;
